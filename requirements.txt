# required for pmsat_learn (copied from pmsat-inference/requirements.txt):
aalpy==1.3.2
matplotlib==3.6.2
Pebble==5.0.3
python-sat[aiger]  # removed optional pblib dependency and version (causes problems on windows)

# additional requirements
# None so far